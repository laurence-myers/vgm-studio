dist/
docs_src/
venv/
*.pyc
.idea/*
.claude/
.junie/
docs/readme.html
*.dro
*.wav

# ...except the committed test fixtures: the DRO/VGM songs the format tests read,
# and the known-good renders `dro-synth`'s render_regression compares against.
!tests/*.dro
!tests/render/*.wav

# Rust
/target/

# egui_kittest snapshot diffs (the committed baselines are the *.png without a
# suffix; these are regenerated on every failing run).
crates/dro-ui/tests/snapshots/*.new.png
crates/dro-ui/tests/snapshots/*.diff.png
crates/dro-ui/tests/snapshots/*.old.png
